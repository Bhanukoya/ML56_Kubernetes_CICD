## Use these terraform files to provsion an EKS cluster along with all the required components to host a web application. Run the belwo commands to provision the infrastructure

terraform init
terraform plan
terraform apply